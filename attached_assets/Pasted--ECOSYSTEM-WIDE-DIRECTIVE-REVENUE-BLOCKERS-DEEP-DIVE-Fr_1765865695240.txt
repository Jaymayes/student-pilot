# 🚨 ECOSYSTEM-WIDE DIRECTIVE: REVENUE BLOCKERS DEEP DIVE 🚨

**From:** CEO, Scholar AI Advisor
**Objective:** Produce a single, data-backed report of **exactly what is preventing revenue growth**. Quantify the impact in dollars ($) and propose the fastest fixes.
**Deadline:** Immediate. Critical blockers (>$5k/mo or P0 risk) escalate now.

---

## 🛑 STEP 1: IDENTIFY YOUR SERVICE
*Look at your `APP_BASE_URL` and execute ONLY your section.*
* **A1** `scholar_auth` → `https://scholar-auth-jamarrlmayes.replit.app`
* **A2** `scholarship_api` → `https://scholarship-api-jamarrlmayes.replit.app`
* **A3** `scholarship_agent` → `https://scholarship-agent-jamarrlmayes.replit.app`
* **A4** `scholarship_sage` → `https://scholarship-sage-jamarrlmayes.replit.app`
* **A5** `student_pilot` → `https://student-pilot-jamarrlmayes.replit.app`
* **A6** `provider_register` → `https://provider-register-jamarrlmayes.replit.app`
* **A7** `auto_page_maker` → `https://auto-page-maker-jamarrlmayes.replit.app`
* **A8** `auto_com_center` → `https://auto-com-center-jamarrlmayes.replit.app`

**ACTION:** Print: `🚩 REVENUE DIAGNOSTIC STARTED: [APP_NAME] @ [APP_BASE_URL]`

---

## 🔍 STEP 2: EXECUTE YOUR REVENUE AUDIT
*Run the specific checks for your app below. Focus on **conversion** and **money**.*

### 🟦 A1 (scholar_auth) - THE GATEKEEPER
* **Revenue Risk:** If users can't login, they can't pay.
* **Checks:**
    1.  **Login Success Rate:** Are legitimate attempts failing? (Check logs for `invalid_grant` or `server_error`).
    2.  **Cross-App Session Loss:** Are users forced to re-login when moving from A7 (landing) to A5 (dashboard)? (Check `SameSite` cookie config).
    3.  **Bot Blocking:** Are we blocking real users (false positives)?

### 🟦 A2 (scholarship_api) - THE PRODUCT
* **Revenue Risk:** If data is stale or API is slow, users churn.
* **Checks:**
    1.  **Data Freshness:** When was the last scholarship sync? (Check DB timestamps).
    2.  **Latency:** Is the `/search` or `/match` endpoint >200ms? (Slow apps kill conversion).
    3.  **Errors:** Rate of 5xx errors on key endpoints.

### 🟦 A3 (scholarship_agent) - THE GROWTH ENGINE
* **Revenue Risk:** If marketing automation fails, traffic dies.
* **Checks:**
    1.  **Campaign Health:** Are emails/SMS actually sending? (Check logs for `sent` vs `failed`).
    2.  **Lead Capture:** Are sign-ups from landing pages actually creating users in the DB?

### 🟦 A4 (scholarship_sage) - THE VALUE PROP
* **Revenue Risk:** If the AI gives bad advice, users won't upgrade.
* **Checks:**
    1.  **Match Quality:** Are we returning 0 results for valid searches?
    2.  **AI Latency:** Is the chat taking >5s to respond?
    3.  **Credits:** Is the credit deduction logic actually working? (Are we giving it away for free?)

### 🟦 A5 (student_pilot) - THE CASH REGISTER (B2C)
* **Revenue Risk:** This is where students pay.
* **Checks:**
    1.  **Stripe Mode:** Is Stripe in `TEST` or `LIVE` mode? (Critical).
    2.  **Checkout Flow:** Can you reach the checkout page? Does the "Pay" button work?
    3.  **Subscription State:** Do active subscriptions correctly unlock features?

### 🟦 A6 (provider_register) - THE CASH REGISTER (B2B)
* **Revenue Risk:** Provider fees (3%) are high-margin.
* **Checks:**
    1.  **Onboarding Friction:** Can a new provider complete registration *without* errors?
    2.  **Listing Creation:** Can they post a scholarship?
    3.  **Fee Collection:** Is the 3% fee logic implemented and active?

### 🟦 A7 (auto_page_maker) - THE TOP OF FUNNEL
* **Revenue Risk:** No traffic = No revenue.
* **Checks:**
    1.  **Indexability:** Is `robots.txt` allowing crawling? Is `sitemap.xml` valid?
    2.  **Page Generation:** Are new SEO pages actually being built/served?
    3.  **CTA Links:** Do the "Apply Now" buttons correctly link to A5/A6 with tracking params?

### 🟦 A8 (auto_com_center) - THE NERVOUS SYSTEM
* **Revenue Risk:** Blindness. We can't fix what we can't see.
* **Checks:**
    1.  **Telemetry:** Are we receiving revenue events from A5/A6?
    2.  **Alerts:** Are we silencing critical payment failure alerts?

---

## 📝 STEP 3: MANDATORY REPORTING FORMAT
*Output this EXACT format. Do not summarize.*

**APP:** [Name]
**REVENUE STATUS:** [🔴 CRITICAL BLOCKER | 🟡 RISK DETECTED | 🟢 FLOWING]

**1. EXECUTIVE SUMMARY**
* **Top Blocker:** [The #1 thing stopping money right now]
* **Est. Monthly Loss:** $[Amount] (Show math: e.g., 1000 users x 5% conversion x $20)
* **Fix ETA:** [Hours/Days]

**2. DETAILED FINDINGS (JSON)**
```json
{
  "service": "[APP_NAME]",
  "blocker_id": "[SHORT_SLUG]",
  "funnel_stage": "[Traffic/Signup/Payment/etc]",
  "severity": "[Critical/High/Medium]",
  "impact_monthly_usd": 0,
  "root_cause": "[Technical explanation]",
  "proposed_fix": "[Actionable step]",
  "owner": "[Role/Service]"
}