# Unified Launch Protocol: Scholar AI Ecosystem (Agent3)

## 🛑 PHASE 1: SELF-IDENTIFICATION (CRITICAL)
**Before executing ANY commands, you must identify your current environment.**

1.  **Scan your environment:** Check your `REPL_SLUG`, `REPL_ID`, or current hostname.
2.  **Match against this Registry:**
    * `scholar_auth` → `https://scholar-auth-jamarrlmayes.replit.app`
    * `scholarship_api` → `https://scholarship-api-jamarrlmayes.replit.app`
    * `scholarship_agent` → `https://scholarship-agent-jamarrlmayes.replit.app`
    * `scholarship_sage` → `https://scholarship-sage-jamarrlmayes.replit.app`
    * `student_pilot` → `https://student-pilot-jamarrlmayes.replit.app`
    * `provider_register` → `https://provider-register-jamarrlmayes.replit.app`
    * `auto_page_maker` → `https://auto-page-maker-jamarrlmayes.replit.app`
    * `auto_com_center` → `https://auto-com-center-jamarrlmayes.replit.app`

3.  **Lock Scope:** Once identified, **ONLY** execute the instructions in the section below matching your App ID. Ignore all other app instructions.
4.  **Mandatory Reporting Header:** Every single output you generate must start with this exact header:
    > **APP:** [Your Detected App Name] | **URL:** [Your Detected Base URL] | **STATUS:** [Status]

---

## 🟢 PHASE 2: APP-SPECIFIC INSTRUCTIONS (EXECUTE YOUR MATCH ONLY)

### 1. IF YOU ARE [scholar_auth]
* **Role:** Identity Provider (OAuth2/OIDC).
* **Autonomy Check:** Verify `GET /health` returns 200. Verify JWKS endpoint `/.well-known/jwks.json` is reachable (this is critical for all other apps).
* **Launch Action:** Ensure token issuance is active. Confirm `RS256` keys are loaded.
* **Report:** Confirm you are ready to sign tokens for the ecosystem.

### 2. IF YOU ARE [scholarship_api]
* **Role:** Central Data API.
* **Autonomy Check:** Verify Database connection. Run a read test on the `scholarships` table. Ensure `GET /api/scholarships` returns JSON.
* **Launch Action:** Verify `scholar_auth` public key is cached for verifying incoming tokens.
* **Report:** Confirm data is serving and secured.

### 3. IF YOU ARE [scholarship_agent]
* **Role:** Growth & Marketing Automation.
* **Autonomy Check:** Verify the `CampaignEngine` is running. Check connection to `scholarship_api` (your data source).
* **Launch Action:** Initialize the job scheduler. Ensure fallback logic is active if API is cold.
* **Report:** Confirm "Marketing Autopilot" is engaged.

### 4. IF YOU ARE [scholarship_sage]
* **Role:** AI Advisory Chatbot.
* **Autonomy Check:** Test OpenAI API connection. Test RAG context retrieval from `scholarship_api`.
* **Launch Action:** Verify "Academic Honesty" guardrails are active (refusal to write essays).
* **Report:** Confirm AI is ready to answer student queries.

### 5. IF YOU ARE [student_pilot]
* **Role:** B2C Student Frontend.
* **Autonomy Check:** Verify `scholar_auth` redirect works. Verify `scholarship_api` data loads in the dashboard.
* **Launch Action:** Check Stripe integration (Test/Live mode). Ensure the "Upgrade" flow is clickable.
* **Traffic Command:** **OPEN THE GATES.** Ensure SEO meta tags are populated.
* **Report:** Confirm "Student Portal Live."

### 6. IF YOU ARE [provider_register]
* **Role:** B2B Provider Frontend.
* **Autonomy Check:** Verify Organization sign-up flow. Verify Stripe Connect onboarding link generation.
* **Launch Action:** Ensure the "3% Platform Fee" logic is active in Stripe configs.
* **Report:** Confirm "Provider Marketplace Open."

### 7. IF YOU ARE [auto_page_maker]
* **Role:** Programmatic SEO Engine.
* **Autonomy Check:** Verify `sitemap.xml` is generated and accessible. Verify dynamic routing for scholarship pages.
* **Launch Action:** **START TRAFFIC INGESTION.** Trigger a sitemap ping to search engines (if configured) or confirm robots.txt allows crawling.
* **Report:** Confirm "SEO Engine Spooling Up."

### 8. IF YOU ARE [auto_com_center]
* **Role:** Central Communications Hub.
* **Autonomy Check:** Verify connection to Email Provider (Postmark/SendGrid) and Twilio (if active). Verify Webhook receiver endpoints are 200 OK.
* **Launch Action:** Process any pending notification queues.
* **Report:** Confirm "Comms Channels Open."

---

## 🚀 PHASE 3: EXECUTION & REPORTING
**Immediate Action Required:**
1.  Perform your specific Autonomy Check defined above.
2.  If successful, declare **"STATUS: OPERATIONAL & AUTONOMOUS"**.
3.  If failed, declare **"STATUS: BLOCKED"** and list the specific missing env var or error.
4.  **GOAL:** We are launching now. Assume production posture.