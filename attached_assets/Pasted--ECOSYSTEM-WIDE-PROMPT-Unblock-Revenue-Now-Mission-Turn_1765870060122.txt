# 🚨 ECOSYSTEM-WIDE PROMPT: Unblock Revenue Now

## 🎯 Mission
Turn on real payments, re-enable automated marketing, refresh inventory, and emit revenue telemetry so cash can flow within 24–48 hours.

## ✅ Success Criteria
1.  **A5:** First successful live B2C charge processed (and refunded) in Stripe Live.
2.  **A6:** At least 3 provider scholarships published and visible.
3.  **A2:** Daily scholarship data sync running; data freshness < 24 hours.
4.  **A8:** Revenue telemetry visible for both B2C charges and B2B fees.

---

## 🔍 STEP 1: IDENTIFY & EXECUTE (Auto-Detect)
*Match your `APP_BASE_URL` below and execute your specific P0 tasks.*

### 🟦 A5 student_pilot (`https://student-pilot-jamarrlmayes.replit.app`)
**Objective:** Payments LIVE and accurate health reporting.
1.  **Verify Stripe Live:** Ensure `STRIPE_SECRET_KEY` starts with `sk_live_`.
2.  **Fix Health Endpoint:** If health says "test_mode" but keys are live, correct the logic.
3.  **Rollout:** Set `STRIPE_ROLLOUT_PCT` to `100`.
4.  **Telemetry:** Emit `revenue.transaction_succeeded` to A8.
5.  **Verify:** Perform a $1.00 live transaction and immediately refund.

### 🟦 A3 scholarship_agent (`https://scholarship-agent-jamarrlmayes.replit.app`)
**Objective:** Email automation ON.
1.  **Secrets:** Insert `SENDGRID_API_KEY`.
2.  **Campaigns:** Enable B2C nurture and B2B provider activation.
3.  **Telemetry:** Emit `marketing.email_sent`.
4.  **Verify:** Send a test email to internal seed list.

### 🟦 A2 scholarship_api (`https://scholarship-api-jamarrlmayes.replit.app`)
**Objective:** Fresh data live and scheduled.
1.  **Manual Refresh:** Run immediate sync.
2.  **Scheduling:** Add daily sync cron/worker.
3.  **Telemetry:** Emit `data.sync_completed`.
4.  **Verify:** Confirm data freshness < 24h.

### 🟦 A6 provider_register (`https://provider-register-jamarrlmayes.replit.app`)
**Objective:** Publish inventory and enable fees.
1.  **Stripe Connect LIVE:** Ensure platform is in Live mode.
2.  **UI Nudges:** Add "Publish Now" CTA for drafts.
3.  **Telemetry:** Emit `provider.scholarship_published`.
4.  **Verify:** Publish 3 drafts; confirm fees captured.

---

## 📝 STEP 2: MANDATORY REPORTING
*Output this format after execution:*

**APP:** [Name]
**REVENUE STATUS:** [🟢 FLOWING | 🔴 BLOCKED]
**EVIDENCE:**
* [ ] Action: (e.g., Stripe Key Checked) -> Result: (sk_live_...)
* [ ] Action: (e.g., Health Endpoint) -> Result: ("stripe": "live")
* [ ] Action: (e.g., Transaction) -> Result: (Charge ID: ch_...)
**NEXT STEP:** [What needs to happen next?]