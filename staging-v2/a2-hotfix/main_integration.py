"""
A2 scholarship_api - main.py integration example

Add these lines to the A2 workspace main.py to wire error handlers:

from middleware.error_handlers import register_error_handlers
app = FastAPI()
register_error_handlers(app)
"""
